# Python Flask Background Signer

## Installation

Run the following in your command prompt:
```shell
pip install -r requirements.txt
```

## Usage

To run the server run:

```shell
flask --app server run
```